name: Deploy a Firebase Hosting

# Publica la app en https://mundialbet-club.web.app en cada push a main.
# GitHub Pages sigue publicando por su cuenta (queda como respaldo).

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-hosting
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: deploy
        continue-on-error: true
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: mundialbet-club

      # Reglas de Firestore: se despliegan con el mismo service account.
      # No hacemos fallar el job si esto falla (el deploy del hosting es lo
      # crítico), pero SI el service account no tiene permiso IAM para
      # firestore:rules (error típico: 403 en serviceusage.googleapis.com),
      # antes esto quedaba invisible — el job reportaba éxito igual y las
      # reglas nunca se publicaban. Ahora queda bien visible en el summary.
      - name: Deploy Firestore rules
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > /tmp/sa.json
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/sa.json
          if npx firebase-tools@latest deploy --only firestore:rules --project mundialbet-club --non-interactive 2>&1 | tee /tmp/rules-deploy.log; then
            echo "✅ Reglas de Firestore publicadas." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## ⚠️ Las reglas de Firestore NO se publicaron" >> "$GITHUB_STEP_SUMMARY"
            echo "El service account no pudo desplegar firestore.rules (ver log abajo)." >> "$GITHUB_STEP_SUMMARY"
            echo "Hay que publicarlas a mano: Firebase Console → Firestore Database → Rules." >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            tail -20 /tmp/rules-deploy.log >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi
          rm -f /tmp/sa.json /tmp/rules-deploy.log

      # La action a veces reporta "failure" aunque el deploy SI se publicó: una
      # reconexión de red ("premature close") hace que firebase-tools reintente
      # una versión que ya había quedado activa, y la API responde "is the
      # current active version". Antes de fallar el job (y mandar correo) de
      # verdad, confirmamos contra el sitio en vivo si el contenido ya coincide.
      - name: Verificar publicación si la action reportó error
        if: steps.deploy.outcome == 'failure'
        run: |
          EXPECTED=$(grep -o "CACHE = '[^']*'" sw.js)
          echo "Buscando en vivo: $EXPECTED"
          for i in 1 2 3 4 5; do
            sleep 6
            LIVE=$(curl -fsS "https://mundialbet-club.web.app/sw.js?cb=$RANDOM-$i" | grep -o "CACHE = '[^']*'" || true)
            echo "Intento $i: $LIVE"
            if [ -n "$LIVE" ] && [ "$LIVE" = "$EXPECTED" ]; then
              echo "El deploy SI se publicó (coincide sw.js). Falso negativo conocido de la action; se trata como éxito."
              exit 0
            fi
          done
          echo "El sitio en vivo no coincide con lo esperado: el deploy realmente falló."
          exit 1
