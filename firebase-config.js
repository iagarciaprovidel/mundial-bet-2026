/* ============================================================
   MundialBet Club 2026 — Configuración de Firebase
   ------------------------------------------------------------
   Pega aquí la config de TU proyecto Firebase:
   Firebase Console → ⚙ Project settings → "Your apps" → Web app → Config.
   NOTA: estos valores (apiKey, etc.) NO son secretos; es seguro
   tenerlos en el front. La seguridad real la dan las reglas de
   Firestore (firestore.rules), no esconder el apiKey.
   ============================================================ */
window.MB_FIREBASE_CONFIG = {
  apiKey: "PEGAR_AQUI",
  authDomain: "PEGAR_AQUI.firebaseapp.com",
  projectId: "PEGAR_AQUI",
  storageBucket: "PEGAR_AQUI.appspot.com",
  messagingSenderId: "PEGAR_AQUI",
  appId: "PEGAR_AQUI",
};
